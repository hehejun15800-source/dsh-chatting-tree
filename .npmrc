# DSH ships the platform libraries the browser bundle requires (React, Cordis, the
# client UI packages). They are resolved through the harness module table at
# runtime, never installed — so this package has no runtime dependencies, and peer
# resolution is only here for editors that read the manifest.
auto-install-peers=false
